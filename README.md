# HighSim Annotated Highway Trajectory Dataset

This dataset is derived from the High-Granularity Highway Simulation (HighSIM) dataset, which contains vehicle trajectories extracted from several high-resolution aerial videos recorded by helicopters along the I-75 highway segment. The data were collected at a sampling rate of 30 Hz, providing high temporal resolution for frame-level driving behavior analysis. This released file further enriches the original trajectory data with unsupervised anomaly annotations generated by the proposed **Utility-enhanced GRU-AE-GMM** framework.

---

## Overview

| Property | Value |
|---|---|
| Total records | 7,568,647 |
| Total vehicles | 2,182 |
| Unique frames | 68,701 |
| Frame ID range | 138,000 – 206,700 |
| Lane numbers | 0, 1, 2, 3 |
| Sampling rate | 30 Hz |

Each row represents **one vehicle at one frame**. Multiple vehicles are present per frame.

---

## Dataset Characteristics

The dataset preserves detailed vehicle information, including speed, acceleration, GPS location and lane information. These variables support driver behavior analysis and trajectory-based anomaly detection research.

The `Anomaly` column provides frame-level anomaly annotations generated by the proposed Utility-enhanced GRU-AE-GMM framework.

| Column | Type | Description |
|---|---|---|
| `Vehicle_ID` | int | Unique vehicle identifier |
| `Global_Time` | float | Timestamp (ms) |
| `Frame_ID` | int | Frame index |
| `Local_X` | float | Lateral position in local coordinate system (ft) |
| `Local_Y` | float | Longitudinal position in local coordinate system (ft) |
| `Global_X` | float | Longitude coordinate |
| `Global_Y` | float | Latitude coordinate |
| `Width` | int | Vehicle width (ft) |
| `Length` | int | Vehicle length (ft) |
| `Class` | int | Vehicle class |
| `Speed` | float | Vehicle speed (ft/s) |
| `Acceleration` | float | Raw acceleration (ft/s²) |
| `Lane_Num` | int | Current lane number |
| `Preceding_Vehicle_ID` | int | Leading vehicle ID |
| `Space_Headway` | float | Gap to leading vehicle (ft) |
| `Acc_from_vsmooth_ftps2` | float | Acceleration derived from smoothed speed (ft/s²) |
| `Acc_from_vsmooth_g` | float | Smoothed acceleration in g-force |
| `Preceding_Speed` | float | Speed of leading vehicle (ft/s) |
| **`Anomaly`** | int | **Model-generated anomaly label (0 = normal, 1 = anomalous)** |

The anomaly labels are generated automatically using an unsupervised learning framework rather than manual human labeling.

Unlike traditional threshold-based methods that mainly focus on extreme acceleration, braking or TTC, the proposed framework captures multi-dimensional abnormal driving patterns by jointly considering:

- speed deviation
- longitudinal-control behavior
- lane-changing behavior
- interaction risk with surrounding vehicles

As a result, the proposed framework can detected anomalies include subtle or compound abnormal driving behaviors that are difficult to identify using fixed kinematic thresholds alone.

---

## Anomaly Detection Pipeline

The anomaly detection framework consists of four main stages:

1. Trajectory preprocessing and feature extraction
2. Utility-based behavioral feature construction
3. GRU-based Autoencoder training for normal behavior learning
4. Gaussian Mixture Model (GMM) clustering and adaptive threshold estimation

The framework first transforms raw trajectory variables into utility-based behavioral features representing:

- speed deviation
- longitudinal-control deviation
- lane deviation
- interaction risk

A GRU-based Autoencoder is then trained using normal driving sequences. The learned latent representations are further modeled using a Gaussian Mixture Model (GMM) to capture heterogeneous normal driving modes.

For each sequence, the Squared Prediction Error (SPE) is compared with the corresponding cluster-specific Upper Control Limit (UCL). Frames whose SPE exceeds the adaptive threshold are labeled as anomalous.
